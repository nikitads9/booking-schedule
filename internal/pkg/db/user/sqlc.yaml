version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "init.sql"
    gen:
      go:
        package: "user"
        sql_package: "pgx/v5"
        emit_db_tags: true
        out: "./"